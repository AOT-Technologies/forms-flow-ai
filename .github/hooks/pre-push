#!/bin/sh
set -eu

# Configuration
GIT_DIR="$(git rev-parse --git-dir)"
FLAG_FILE="$GIT_DIR/hooks/.skip-snyk"
SNYK_TOKEN="c0ed54ed-a982-4c06-aa6f-3ae5d10a0213"

# Check if user opted out
if [ -f "$FLAG_FILE" ]; then
    choice=$(cat "$FLAG_FILE" 2>/dev/null || echo "")
    rm -f "$FLAG_FILE"
    if [ "$choice" = "skip" ]; then
        echo "⚠️  User chose to skip Snyk scan. Proceeding with push..." > /dev/tty
        exit 0
    fi
fi

# Function to detect OS and set Snyk command
setup_snyk_command() {
    if command -v snyk >/dev/null 2>&1; then
        SNYK_CMD="snyk"
    else
        echo "⚠️  Snyk CLI not found. Attempting to install..." > /dev/tty

        # Install via npm if available
        if command -v npm >/dev/null 2>&1; then
            echo "📦 Installing Snyk CLI via npm..." > /dev/tty
            npm install -g snyk >/dev/null 2>&1
        fi

        # If still not found, try downloading binary
        if ! command -v snyk >/dev/null 2>&1; then
            echo "⬇️  Downloading Snyk CLI binary..." > /dev/tty
            if command -v curl >/dev/null 2>&1; then
                curl -sL https://static.snyk.io/cli/latest/snyk-linux -o /usr/local/bin/snyk
            elif command -v wget >/dev/null 2>&1; then
                wget -qO /usr/local/bin/snyk https://static.snyk.io/cli/latest/snyk-linux
            else
                echo "❌ Neither curl nor wget is available. Please install Snyk manually." > /dev/tty
                return 1
            fi
            chmod +x /usr/local/bin/snyk
        fi

        # Final check
        if command -v snyk >/dev/null 2>&1; then
            echo "✅ Snyk CLI installed successfully." > /dev/tty
            SNYK_CMD="snyk"
        else
            echo "❌ Failed to install Snyk. Please install manually:" > /dev/tty
            echo "   npm install -g snyk" > /dev/tty
            echo "   OR download from: https://github.com/snyk/snyk" > /dev/tty
            return 1
        fi
    fi

    # Authenticate Snyk
    if [ -n "$SNYK_TOKEN" ]; then
        echo "🔐 Authenticating Snyk with token..." > /dev/tty
        $SNYK_CMD auth "$SNYK_TOKEN" >/dev/null 2>&1
        if [ $? -eq 0 ]; then
            echo "✅ Snyk authenticated successfully." > /dev/tty
        else
            echo "❌ Failed to authenticate Snyk. Check your token." > /dev/tty
            return 1
        fi
    else
        echo "⚠️  No SNYK_TOKEN provided. Please run 'snyk auth' manually or set the token." > /dev/tty
    fi

    return 0
}


# Function to check prerequisites
check_prerequisites() {
    echo "🔍 Checking prerequisites..." > /dev/tty
    
    # Check if Maven is available for Java projects
    if find . -name "pom.xml" -type f | head -1 | grep -q .; then
        if ! command -v mvn >/dev/null 2>&1; then
            echo "  Maven not found but pom.xml files detected." > /dev/tty
            echo "   Installing Maven is recommended for complete scanning." > /dev/tty
            echo "   Download from: https://maven.apache.org/download.cgi" > /dev/tty
            return 1
        fi
    fi
    
    # Check for package.json/package-lock.json sync issues
    if [ -f "package.json" ] && [ -f "package-lock.json" ]; then
        echo "📦 Checking package.json sync..." > /dev/tty
        if ! npm ls >/dev/null 2>&1; then
            echo "⚠️  package.json and package-lock.json might be out of sync." > /dev/tty
            echo "   Consider running 'npm install' to fix." > /dev/tty
            return 1
        fi
    fi
    
    return 0
}

# Function to run Snyk test with better filtering
run_snyk_test() {
    echo "🔍 Running Snyk vulnerability test..." > /dev/tty
    
    # Test only the main project first (most reliable)
    if "$SNYK_CMD" test --severity-threshold=high 2>/dev/null; then
        echo "✅ No high/critical vulnerabilities found in main project." > /dev/tty
        return 0
    else
        test_exit_code=$?
        case $test_exit_code in
            1)
                echo "🚨 High or critical vulnerabilities found in main project!" > /dev/tty
                echo "   View details: $SNYK_CMD test" > /dev/tty
                ;;
            2)
                echo "⚠️  Snyk test configuration issues detected." > /dev/tty
                ;;
            3)
                echo "ℹ️  No supported projects found for main test." > /dev/tty
                ;;
            *)
                echo "⚠️  Snyk test completed with warnings (exit code: $test_exit_code)." > /dev/tty
                ;;
        esac
        return 0  # Don't block push for test issues
    fi
}

# Function to run Snyk monitor with better exclusions
run_snyk_monitor() {
    echo "📡 Sending project snapshot to Snyk (this may take a moment)..." > /dev/tty
    
    # More targeted exclusions to avoid problematic files
    EXCLUDE_PATTERNS="**/node_modules/**,**/vendor/**,**/target/**,**/.git/**,**/scripts/**,**/project.json"
    
    # Try monitor with exclusions first
    if "$SNYK_CMD" monitor \
        --all-projects \
        --detection-depth=2 \
        --exclude="$EXCLUDE_PATTERNS" \
        --skip-unresolved \
        >/dev/null 2>&1; then
        echo "✅ All projects successfully sent to Snyk for monitoring." > /dev/tty
        return 0
    fi
    
    # If that fails, try a more conservative approach
    echo "⚠️  Full monitor failed, trying main project only..." > /dev/tty
    if "$SNYK_CMD" monitor --skip-unresolved >/dev/null 2>&1; then
        echo "✅ Main project sent to Snyk for monitoring." > /dev/tty
        return 0
    fi
    
    # If still failing, just warn and continue
    echo "⚠️  Monitor had issues but continuing. Check dashboard manually:" > /dev/tty
    echo "   https://app.snyk.io/org/alanraju-aot" > /dev/tty
    return 0
}

# Function to provide helpful tips based on detected issues
provide_tips() {
    echo "" > /dev/tty
    echo "💡 Tips for better Snyk scanning:" > /dev/tty
    
    # Check for specific issues and provide targeted advice
    if find . -name "pom.xml" -type f | head -1 | grep -q . && ! command -v mvn >/dev/null 2>&1; then
        echo "   • Install Maven for Java project scanning" > /dev/tty
    fi
    
    if [ -f "package.json" ] && [ -f "package-lock.json" ]; then
        echo "   • Keep package.json and package-lock.json in sync with 'npm install'" > /dev/tty
    fi
    
    if find . -name "requirements.txt" -type f | head -1 | grep -q .; then
        echo "   • Ensure Python virtual environment is activated for Python scanning" > /dev/tty
    fi
    
    echo "   • Review findings at: https://app.snyk.io/org/alanraju-aot" > /dev/tty
    echo "" > /dev/tty
}

# Main execution
echo "🛡️  Starting Snyk security scan..." > /dev/tty

# Setup Snyk command
if ! setup_snyk_command; then
    echo "⚠️  Skipping Snyk scan due to setup issues." > /dev/tty
    exit 0
fi

# Check authentication
if ! "$SNYK_CMD" auth --quiet >/dev/null 2>&1; then
    echo "❌ Snyk not authenticated. Run: $SNYK_CMD auth" > /dev/tty
    echo "⚠️  Skipping scan due to authentication failure." > /dev/tty
    exit 0
fi

# Check prerequisites (non-blocking)
check_prerequisites || {
    echo "⚠️  Some prerequisites missing but continuing..." > /dev/tty
}

# Run Snyk test (non-blocking)
run_snyk_test

# Run Snyk monitor (non-blocking)
run_snyk_monitor

# Provide helpful tips
provide_tips

echo "🎉 Security scan completed. Push proceeding..." > /dev/tty
exit 0