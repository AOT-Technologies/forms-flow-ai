---
kind: Template
apiVersion: v1
metadata:
  name: forms-flow-forms
  annotations:
    description: ""
    tags: forms-flow-forms
objects:
-
  apiVersion: build.openshift.io/v1
  kind: BuildConfig
  metadata:
    name: forms-flow-forms
    labels:
      app: forms-flow-forms
  spec:
    source:
      git:
        ref: master
        uri: 'https://github.com/AOT-Technologies/forms-flow-ai'
      contextDir: forms-flow-forms
    output:
      to:
        kind: ImageStreamTag
        name: 'forms-flow-forms:latest'
    strategy:
      type: Docker
      dockerStrategy:
        dockerfilePath: Dockerfile
    triggers:
      - type: ImageChange
        imageChange: {}
      - type: ConfigChange

- 
  apiVersion: image.openshift.io/v1
  kind: ImageStream
  metadata:
    name: forms-flow-forms