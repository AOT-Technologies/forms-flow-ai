from enum import Enum


class WebApiTables(Enum):
    """Enum for managing Web API table names."""

    APPLICATION = "application"
    AUTHORIZATION = "authorization"
    FORM_PROCESS_MAPPER = "form_process_mapper"
